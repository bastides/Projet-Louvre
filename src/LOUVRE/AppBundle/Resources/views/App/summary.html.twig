{% extends "LOUVREAppBundle::layout.html.twig" %}

{% block title %} {{ parent() }} - Récapitulatif {% endblock %}

{% block stylesheets %} {{ parent() }} {% endblock %}

{% block body %}
    <div class="container">

        <div class="row">
            <h2>Votre commande</h2>
        </div>

        <div class="row">
            <div class="col-md-offset-2 col-md-8" id="summary">
                <table class="table table-hover table-bordered">
                    <thead>
                    <tr>
                        <th id="table-head">Commande N° {{ command.bookingCode }}</th>
                    </tr>
                    </thead>
                    <tbody id="table-body">
                    {% for ticket in listTickets %}
                        <tr>
                            <td><span class="bold">{{ ticket.ticketname }}</span> {{ ticket.lastname }} {{ ticket.firstname }}</td>
                            <td class="text-center"> {{ ticket.price }} € </td>
                        </tr>
                    {% endfor %}

                    <tr>
                        <td class="text-right bold">Total</td>
                        <td class="text-center bold"> {{ command.totalprice }} € </td>
                    </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <div id="summary-buttons">
            <div class="row">
                <div class="col-xs-12 col-md-offset-4 col-md-4 payment-button">
                    <a href="{{ path('louvre_paypal_payment_prepare') }}" class="btn btn-primary btn-block text-center">PAYPAL</a>
                </div>
            </div>
            <div class="row">
                <div class="col-xs-12 col-md-offset-4 col-md-4 payment-button">
                    <a href="{{ path('louvre_app_home') }}" class="btn btn-primary btn-block text-center">CARTE BANCAIRE</a>
                </div>
            </div>
        </div>

    </div>
{% endblock %}

{% block javascripts %} {{ parent() }} {% endblock %}